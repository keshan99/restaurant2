# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration
PORT=3000

# GCS bucket for image uploads (private – no public IAM). Only the backend uploads and serves signed URLs.
GCS_BUCKET_NAME=restaurant-files-001

# When using user ADC locally (no service account key), set this so image URLs use the backend proxy instead of signed URLs.
# BACKEND_URL=http://localhost:3000

# Database Pool Configuration (optional)
POOL_MAX=10

# Instructions:
# 1. Copy this file to .env
# 2. Replace DATABASE_URL with your actual Supabase/PostgreSQL connection string
# 3. Get connection string from Supabase: Project Settings > Database > Connection string
